'use strict';

describe('app.quiz module', function() {

  beforeEach(module('app.quiz'));

  describe('quiz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var quizCtrl = $controller('QuizCtrl');
      expect(quizCtrl).toBeDefined();
    }));

  });
});
